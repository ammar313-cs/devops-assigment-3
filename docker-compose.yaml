---
version: "3.8"
services:
  backend:
    container_name: backend
    image: react_app
    ports:
      - 3000:3000
    command: npx nodemon index.js
    volumes:
      - ./MyApp:/MyApp
  frontend:
    container_name: frontend
    image: react_app
    ports:
      - 443:3000
    command: npm start
    volumes:
      - ./MyApp:/MyApp

